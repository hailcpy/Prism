[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prism-sdk"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.40",
    "httpx>=0.27",
]

[tool.hatch.build.targets.wheel]
packages = ["src/prism_sdk"]
