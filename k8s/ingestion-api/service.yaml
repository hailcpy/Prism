apiVersion: v1
kind: Service
metadata:
  name: ingestion-api
  namespace: prism
spec:
  type: NodePort
  selector:
    app: ingestion-api
  ports:
  - port: 8001
    targetPort: 8001
    nodePort: 30101
    protocol: TCP
    name: http
