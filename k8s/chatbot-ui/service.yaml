apiVersion: v1
kind: Service
metadata:
  name: chatbot-ui
  namespace: prism
spec:
  type: NodePort
  selector:
    app: chatbot-ui
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30001
    protocol: TCP
    name: http
