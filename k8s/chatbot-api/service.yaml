apiVersion: v1
kind: Service
metadata:
  name: chatbot-api
  namespace: prism
spec:
  type: NodePort
  selector:
    app: chatbot-api
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30100
    protocol: TCP
    name: http
