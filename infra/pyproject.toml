[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prism-infra"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "redis>=5.0",
]

[tool.hatch.build.targets.wheel]
packages = ["prism_infra"]
